{
  "name": "whatsapp-sticker-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "for file in api/webhook.mjs api/admin/*.mjs api/cron/*.mjs lib/*.mjs; do node --check \"$file\" || exit 1; done",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "@vercel/functions": "^3.7.6",
    "@supabase/supabase-js": "^2.53.0",
    "sharp": "^0.35.3"
  }
}
